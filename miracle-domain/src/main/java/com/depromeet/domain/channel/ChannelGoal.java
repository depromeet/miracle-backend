package com.depromeet.domain.channel;


import javax.persistence.Entity;

/**
 * 목표 = 채널 이름 + 소개라고 생각하고 있는데, 별도라면 생길 예정
 * (채널에 1개의 목표가 있을 건지, n개의 목표가 있을건지에 따라 연관관계 매핑 달라짐)
 */
@Entity
public class ChannelGoal {
}
